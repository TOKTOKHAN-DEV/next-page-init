import { volumeUpObject } from '@toktokhan-dev/universal'

import { colors as colorSchema } from '@/generated/tokens/colors'

const colors = { ...volumeUpObject('.', colorSchema) }

export default colors
